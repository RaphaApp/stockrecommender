streamlit>=1.40
pandas
numpy
altair

# Optional: Postgres/Supabase backend (DB_BACKEND="postgres").
# Without it the app silently uses local SQLite.
psycopg[binary]

# Optional: PDF ingestion (Workstream 4). Without it, PDF uploads report a clear
# message and image uploads still work.
pymupdf